additionalProperties:
  formFields:
    - default: Asia/Shanghai
      edit: true
      envKey: PANEL_APP_TIME_ZONE
      labelEn: TZ
      labelZh: 时区
      label:
        en: TZ
        zh: 时区
      required: true
      type: text
    - default: admin
      edit: true
      envKey: PANEL_AUTH_USERNAME
      labelEn: Admin Username
      labelZh: 管理员用户名
      label:
        en: Admin Username
        zh: 管理员用户名
      required: true
      type: text
    - default: admin
      edit: true
      envKey: PANEL_AUTH_PASSWORD
      labelEn: Admin Password
      labelZh: 管理员密码
      label:
        en: Admin Password
        zh: 管理员密码
      random: true
      required: true
      type: password
    - default: ""
      edit: true
      envKey: PANEL_DB_HOST
      key: postgresql
      labelEn: Database Service
      labelZh: 数据库服务
      label:
        en: Database Service
        zh: 数据库服务
      required: true
      type: service
    - default: cypht
      edit: true
      envKey: PANEL_DB_NAME
      labelEn: Database Name
      labelZh: 数据库名
      label:
        en: Database Name
        zh: 数据库名
      random: true
      required: true
      rule: paramCommon
      type: text
    - default: ""
      edit: true
      envKey: PANEL_DB_USER
      labelEn: Database User Name
      labelZh: 数据库用户名
      label:
        en: Database User Name
        zh: 数据库用户名
      random: true
      required: true
      rule: paramCommon
      type: text
    - default: ""
      edit: true
      envKey: PANEL_DB_PASSWORD
      labelEn: Database User Password
      labelZh: 数据库密码
      label:
        en: Database User Password
        zh: 数据库密码
      random: true
      required: true
      type: password
    - default: ""
      edit: true
      envKey: PANEL_REDIS_HOST
      key: redis
      labelEn: Redis Service
      labelZh: 缓存服务
      label:
        en: Redis Service
        zh: 缓存服务
      required: true
      type: service
    - default: ""
      edit: true
      envKey: PANEL_REDIS_PASSWORD
      labelEn: Redis Password
      labelZh: 缓存服务密码
      label:
        en: Redis Password
        zh: 缓存服务密码
      required: true
      type: password
    - default: ""
      edit: true
      envKey: PANEL_OUTLOOK_CLIENT_ID
      labelEn: Outlook Client ID
      labelZh: Outlook OAuth Client ID（可选，Azure 应用注册获取）
      label:
        en: Outlook Client ID (optional)
        zh: Outlook OAuth Client ID（可选）
      required: false
      type: text
    - default: ""
      edit: true
      envKey: PANEL_OUTLOOK_CLIENT_SECRET
      labelEn: Outlook Client Secret
      labelZh: Outlook OAuth Client Secret（可选）
      label:
        en: Outlook Client Secret (optional)
        zh: Outlook OAuth Client Secret（可选）
      required: false
      type: password
    - default: ""
      edit: true
      envKey: PANEL_OUTLOOK_CLIENT_URI
      labelEn: Outlook Redirect URI
      labelZh: Outlook 回调地址（不带查询字符串，如 http://your-domain:port）
      label:
        en: Outlook Redirect URI (no query string, e.g. http://your-domain:port)
        zh: Outlook 回调地址（不带查询字符串，如 http://your-domain:port）
      required: false
      type: text
    - default: ""
      edit: true
      envKey: PANEL_GMAIL_CLIENT_ID
      labelEn: Gmail Client ID
      labelZh: Gmail OAuth Client ID（可选，Google Cloud 控制台获取）
      label:
        en: Gmail Client ID (optional)
        zh: Gmail OAuth Client ID（可选）
      required: false
      type: text
    - default: ""
      edit: true
      envKey: PANEL_GMAIL_CLIENT_SECRET
      labelEn: Gmail Client Secret
      labelZh: Gmail OAuth Client Secret（可选）
      label:
        en: Gmail Client Secret (optional)
        zh: Gmail OAuth Client Secret（可选）
      required: false
      type: password
    - default: ""
      edit: true
      envKey: PANEL_GMAIL_CLIENT_URI
      labelEn: Gmail Redirect URI
      labelZh: Gmail 回调地址（如 http://your-domain:port/?page=home）
      label:
        en: Gmail Redirect URI (e.g. http://your-domain:port/?page=home)
        zh: Gmail 回调地址（如 http://your-domain:port/?page=home）
      required: false
      type: text
    - default: 6020
      edit: true
      envKey: PANEL_APP_PORT_HTTP
      labelEn: Port
      labelZh: 端口
      label:
        en: Port
        zh: 端口
      required: true
      rule: paramPort
      type: number